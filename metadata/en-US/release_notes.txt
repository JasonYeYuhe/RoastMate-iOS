Voice input is fixed.

• The microphone wasn't actually capturing anything — the audio session was never configured, so tapping the mic opened the sheet and no text ever appeared. Voice venting works now.
• Pro subscribers using the roommate group are now correctly counted against the Pro allowance instead of the free one.
• The shareable card is further hardened: it renders only your polished, sendable reply, its text can't be edited before sharing, and names, phone numbers, ID numbers and WeChat/QQ contacts are stripped on-device before anything is drawn.
