{
  "_comment": "RoastMate remote kill-switch. Edit a value + commit; it propagates to clients on their next launch (~1-2 min after GitHub Pages rebuilds). RESTRICT-only: flags can disable a feature or force on-device, but NEVER send user text to cloud without the in-app 5.1.2(i) consent grant. Source of truth: research/web/roastmate-config.json in JasonYeYuhe/RoastMate-iOS; mirrored to the Pages repo by .github/workflows/mirror-research-form-to-pages.yml. See Shared/Services/RemoteConfig.swift + docs/HEALTH_AUDIT_2026-05-29.md §4.",
  "_cloud_sendable_locales_note": "RETIRED — do not use. cloud_sendable_enabled must stay false forever: every shipped binary's consent sheet promises Calm/Sharp/Savage never use cloud, and old installs read this same global flag, so turning it on would route their text off-device against that consent (App Store 5.1.2(i)). A future sendable-cloud feature needs a NEW flag + fresh versioned consent. CI rejects any value but false. See docs/HANDOFF_v1.6_NEXT_SESSION.md.",
  "config_version": 1,
  "echoes_enabled": true,
  "roommate_group_enabled": true,
  "force_local_only": false,
  "vent_cloud_enabled": true,
  "cloud_sendable_enabled": false,
  "share_card_enabled": false,
  "share_card_setup_enabled": false,
  "share_card_visible": true,
  "min_supported_build": 0
}
