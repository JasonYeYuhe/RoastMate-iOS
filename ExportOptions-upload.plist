<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Like ExportOptions.plist but with destination=upload, so
	     `xcodebuild -exportArchive` uploads to App Store Connect DIRECTLY
	     (via the ASC API key passed on the command line) — no altool, no
	     keychain. Used by scripts/build-upload-asc.sh. Automatic signing +
	     -allowProvisioningUpdates lets Xcode fetch/manage the distribution
	     (and, for macOS, the Mac Installer) cert via the API — so the manual
	     roastmate-signing temp keychain is not needed. -->
	<key>method</key>
	<string>app-store-connect</string>
	<key>teamID</key>
	<string>KHMK6Q3L3K</string>
	<key>uploadSymbols</key>
	<true/>
	<key>signingStyle</key>
	<string>automatic</string>
	<key>destination</key>
	<string>upload</string>
</dict>
</plist>
